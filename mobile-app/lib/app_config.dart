class AppConfig {
  static const String instagramUrl = "https://www.instagram.com/direct/inbox/";

  // DM 전용 모드 활성화 여부
  static bool isDmOnlyMode = true;

  // 리다이렉트 주기 (ms)
  static const int redirectIntervalMs = 500;
}
